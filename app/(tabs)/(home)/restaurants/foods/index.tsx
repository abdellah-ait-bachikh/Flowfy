import { StyleSheet, Text, View } from 'react-native'
import React from 'react'
import AppText from '@/components/app/share/AppText'

const FoodsList = () => {
  return (
    <View>
      <AppText>FoodsList</AppText>
    </View>
  )
}

export default FoodsList

const styles = StyleSheet.create({})